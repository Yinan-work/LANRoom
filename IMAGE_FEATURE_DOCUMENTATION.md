# 局域网聊天室图片发送功能文档

## 核心功能介绍

为了让局域网内的设备（包括 Windows 电脑、Android 和 iOS 设备）能够顺畅地收发图片，我们在原有的纯文本聊天基础上新增了强大的图片收发与管理功能：

1.  **多种发图交互**：
    *   **点击发图**：点击输入框左侧的“🖼️”图标，可以从本地相册或文件管理器选择图片发送。
    *   **拖拽发图**：在电脑端，支持将图片文件直接拖拽至聊天页面进行快速发送。
    *   **粘贴发图**：支持截屏后直接在聊天页面使用 `Ctrl+V` (或 `Cmd+V`) 进行无缝粘贴发送。
2.  **移动端优化（智能前端压缩）**：
    *   默认开启前端 Canvas 压缩。自动将用户选择的超大原图（尤其是手机拍摄的 5MB-20MB 的高清照片）按比例压缩（最大宽度限制在 1920px，画质 0.7）。
    *   这一机制使得移动端发图达到了“秒发”的体验，同时也极大地降低了局域网 Wi-Fi 的带宽压力，避免了由于加载超大图片导致的浏览器崩溃卡顿。
3.  **原图发送选项**：
    *   考虑到某些特殊场景（例如需要发送带文字的清晰截图，或设计图等），在输入框左侧保留了“**原图**”复选框。
    *   勾选后，前端不再进行任何处理，直接将未经压缩的文件上传给服务器。
4.  **无上限尺寸限制**：
    *   在服务器端并未硬性限制图片的体积大小，以满足极个别的超大文件传输需求。

## 图片管理与自动清理机制 (防塞满硬盘)

由于本系统主要面向即时局域网沟通，图片数据的永久保留通常是不必要的。为了避免长期使用后图片占满主机硬盘空间，我们采用了**“按启动批次独立管理，自动轮转清理”**的机制：

*   **隔离存放**：每次通过 `一键启动聊天室.bat` 启动服务器时，Node.js 都会在 `public/uploads/` 目录下自动创建一个以当前时间戳命名的独立子文件夹（例如：`run_1712039912345/`）。本次运行期间产生的所有图片都会存放于此。
*   **满三清理**：当系统再次启动并准备新建文件夹时，会自动检测 `uploads` 目录下以 `run_` 开头的文件夹数量。如果检测到历史文件夹加上新建立的文件夹总数将超过 3 个，系统会自动将最老的一批文件夹**整体彻底删除**。
*   **效果**：这种机制保证了你的硬盘上永远最多只保留最后 **3 次启动** 产生的聊天图片，全程静默自动清理，无需人工干预。

## 技术栈与依赖调整

*   **引入 `multer` 依赖**：利用 `npm install multer` 安装了 Node.js 社区内非常流行的处理 `multipart/form-data` 的中间件。它负责安全、高效地接收客户端上传的文件流，并将其写入指定的 `run_xxx` 目录。
*   **前后端数据格式更新**：为了兼容图片和文本的混合传输，WebSocket 传输的数据结构由原来的纯字符串升级为了 JSON 对象。
    *   以前：`socket.emit('chat message', "你好")`
    *   现在：`socket.emit('chat message', { type: "text", content: "你好" })` 或 `{ type: "image", content: "/uploads/run_xxx/img.png" }`
    *   我们在后端做了相应的兼容处理，以防意外的旧数据格式导致异常。
